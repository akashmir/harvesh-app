# Cloud Build configuration for Ultra Crop Recommender - Optimized
steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'build'
      - '-f'
      - 'backend/Dockerfile.ultra-crop-optimized'
      - '-t'
      - 'gcr.io/$PROJECT_ID/ultra-crop-recommender:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/ultra-crop-recommender:latest'
      - './backend'
    dir: '.'
    timeout: '1200s'

  # Push the Docker image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'push'
      - 'gcr.io/$PROJECT_ID/ultra-crop-recommender:$COMMIT_SHA'
    timeout: '600s'

  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'push'
      - 'gcr.io/$PROJECT_ID/ultra-crop-recommender:latest'
    timeout: '600s'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'ultra-crop-recommender-api'
      - '--image'
      - 'gcr.io/$PROJECT_ID/ultra-crop-recommender:$COMMIT_SHA'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '5020'
      - '--memory'
      - '4Gi'
      - '--cpu'
      - '4'
      - '--max-instances'
      - '20'
      - '--min-instances'
      - '1'
      - '--concurrency'
      - '50'
      - '--timeout'
      - '600'
      - '--service-account'
      - 'ultra-crop-sa@$PROJECT_ID.iam.gserviceaccount.com'
      - '--set-env-vars'
      - 'ENVIRONMENT=production,API_HOST=0.0.0.0,API_PORT=5020,DEBUG=false,MODEL_PATH=/app/models,CACHE_TTL=3600,ULTRA_REQUEST_TIMEOUT=10.0'
      - '--set-secrets'
      - 'OPENWEATHER_API_KEY=openweather-api-key:latest,GOOGLE_EARTH_ENGINE_KEY=google-earth-engine-key:latest,BHUVAN_API_KEY=bhuvan-api-key:latest,SECRET_KEY=secret-key:latest'
      - '--cpu-throttling'
      - '--execution-environment'
      - 'gen2'
    timeout: '1200s'

# Store images in Container Registry
images:
  - 'gcr.io/$PROJECT_ID/ultra-crop-recommender:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/ultra-crop-recommender:latest'

# Build options
options:
  machineType: 'E2_HIGHCPU_8'
  diskSizeGb: 100
  logging: CLOUD_LOGGING_ONLY

# Timeout for the entire build
timeout: '1800s'

# Substitution variables
substitutions:
  _SERVICE_NAME: 'ultra-crop-recommender-api'
  _REGION: 'us-central1'
  _MEMORY: '4Gi'
  _CPU: '4'
  _MAX_INSTANCES: '20'
  _MIN_INSTANCES: '1'
